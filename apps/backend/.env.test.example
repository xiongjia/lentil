# Backend environment configuration for tests

# Server port
PORT=3990

# Logging level: trace, debug, info, warn, error, fatal
LOG_LEVEL=debug

# Database (SQLite in-memory for tests)
DB_TYPE=libsql
DB_NAME=:memory: